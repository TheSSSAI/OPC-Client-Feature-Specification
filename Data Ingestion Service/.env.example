# DataIngestionService Environment Variables

# TimescaleDB/PostgreSQL Connection Details
# These are used by the docker-compose.yml file to configure both
# the database container and the ingestion service container.
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=changeme
DB_NAME=opc-twin

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:8080;https://+:8081

# Kestrel Certificate Configuration
# This password is used to decrypt the server's .pfx certificate.
# Ensure this matches the password used to create the certificate.
KESTREL_CERTIFICATE_PASSWORD=changeme

# Logging Configuration
# Example: "Information" or "Debug"
LOG_LEVEL=Information