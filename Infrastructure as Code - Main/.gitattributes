# Normalize line endings to LF for consistency across different operating systems.
# This prevents issues with developers using a mix of Windows, macOS, and Linux.
* text=auto eol=lf

# Explicitly set file types that should always have LF line endings.
*.tf text eol=lf
*.tfvars text eol=lf
*.sh text eol=lf
*.py text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.md text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
.editorconfig text eol=lf

# Specify binary files to prevent them from being treated as text.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary