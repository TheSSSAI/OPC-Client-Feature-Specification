# This file tells git which files and directories to ignore in your project.

# Local Terraform directories
.terraform/
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data.
# Use .tfvars.example files to document required variables.
*.tfvars
*.tfvars.json

# Exclude secrets files.
secrets.yaml
secrets.json
*.pem
*.key

# Ignore override files as they are typically used for local testing.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform provider plugins
# If you wish to commit them, remove the following line.
# However, it's a best practice to let `terraform init` manage them.
.terraform/plugins/

# Terraform plan files
# Plan files can contain sensitive information and should not be committed.
*.tfplan

# Mac OS specific files
.DS_Store
*.swp
*~

# Windows specific files
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE / Editor specific files
.vscode/
.idea/
*.iml

# Generated files for testing
terraform.tfstate.d/

# Debug files
*.log