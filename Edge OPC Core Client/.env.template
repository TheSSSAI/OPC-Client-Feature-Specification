# OPC Core Client Environment Variables Template
# Copy this file to .env and fill in the values for local development.

# --- Client Identity ---
CLIENT_ID=client-001
TENANT_ID=tenant-a
# A one-time token for the initial provisioning process (REQ-1-082)
PROVISIONING_TOKEN=your_one_time_provisioning_token_here

# --- MQTT Broker Configuration (for docker-compose) ---
MQTT_BROKER_HOST=mqtt-broker
MQTT_BROKER_PORT=1883
# Use 'host.docker.internal' if connecting to a broker on the host machine from the container

# --- gRPC Data Ingestion Service Endpoint ---
GRPC_ENDPOINT=http://host.docker.internal:5001

# --- .NET Environment ---
DOTNET_ENVIRONMENT=Development